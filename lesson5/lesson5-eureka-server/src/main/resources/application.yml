spring:
  application:
    name: spring-cloud-eureka-server

management:
  security:
    enabled: false

##公用 Eureka 配置
eureka:
  client:
    ## 向注册中心注册
    register-with-eureka: true
    ##从注册中心获取注册信息（服务、实例信息）
    fetch-registry: true